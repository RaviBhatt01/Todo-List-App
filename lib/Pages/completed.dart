import 'package:flutter/material.dart';

class completedLists extends StatefulWidget {
  const completedLists({super.key});

  @override
  State<completedLists> createState() => _completedListsState();
}

class _completedListsState extends State<completedLists> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue[100],

    );
  }
}
